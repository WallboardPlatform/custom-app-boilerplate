{
	"contractVersion": 1,
	"binding": {
		"property": "skillData",
		"dataPickerType": "any"
	},
	"source": {
		"contract": "EXISTING",
		"sampleData": "sample-datasource.json"
	},
	"delivery": {
		"suggestedDatasourceName": "Call Center Skill States",
		"quickEditEligible": false
	},
	"acceptedValueShapes": [
		"object containing keyed per-agent rows under data",
		"direct object keyed by unique agent",
		"array of per-agent skill rows",
		"JSON string of any documented shape"
	],
	"behavior": {
		"unbound": "render representative sample data for local evaluation",
		"boundEmpty": "filter placeholder sentinels and render the configured empty state when no real rows remain",
		"invalidRows": "discard rows without both skill and agent names",
		"ordering": "group rows by skill, then sort skills and agents alphabetically"
	}
}
